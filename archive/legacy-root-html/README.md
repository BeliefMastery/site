# Legacy root HTML (archived)

| File | Note |
|------|------|
| `index-full-legacy.html` | Former repository root `index.html` (large marketing portal). |

Canonical site entry is now the V3 SPA at **`/site/`** (repository root `index.html` + `assets/`).

Engine pages that lived at the repository root (`diagnosis.html`, etc.) were moved here as `*-legacy.html`; URLs at the root are thin redirects to the SPA (`#/engines/...`).
